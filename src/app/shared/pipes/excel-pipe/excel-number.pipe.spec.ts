import { ExcelNumberPipe } from './excel-number.pipe';

describe('ExcelNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcelNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
